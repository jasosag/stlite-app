# En Streamlit Cloud no se fija el puerto: la plataforma elige el suyo.
# En tu Mac, iniciar.sh sigue usando --server.port 8517.

[browser]
gatherUsageStats = false

[theme]
primaryColor = "#0f766e"
backgroundColor = "#f8fafc"
secondaryBackgroundColor = "#ffffff"
textColor = "#0f172a"
font = "sans serif"
